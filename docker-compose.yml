services:
  postgres:
    image: postgres:15-alpine
    container_name: trailmix-postgres
    environment:
      - POSTGRES_USER=trailmix
      - POSTGRES_PASSWORD=wayfarers
      - POSTGRES_DB=trailmix_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - trailmix-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trailmix -d trailmix_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: trailmix-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - trailmix-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: trailmix-backend
    ports:
      - "8000:8000"
    volumes:
      # Mount secrets directory for Firebase credentials and .env
      # Backend code looks for secrets at /app/secrets (3 levels up from backend/accounts/signups.py)
      - ./secrets:/app/secrets:ro
      # Mount uploads directory to persist uploaded files
      - ./backend/uploads:/app/uploads
    environment:
      - CORS_ALLOW_ORIGINS=*
    env_file:
      - ./secrets/.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/docs')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - trailmix-network

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: trailmix-cloudflared
    command: tunnel --url http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - trailmix-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # Logs will contain the tunnel URL - we'll extract it via docker logs

  tunnel-url-extractor:
    image: python:3.11-alpine
    container_name: trailmix-tunnel-url-extractor
    volumes:
      # Mount workspace root to write .tunnel-url files
      - .:/workspace
      # Mount Docker socket to read cloudflared logs
      - /var/run/docker.sock:/var/run/docker.sock:ro
    working_dir: /workspace
    command: >
      sh -c "
        apk add --no-cache docker-cli &&
        python3 extract-tunnel-url.py
      "
    depends_on:
      cloudflared:
        condition: service_started
    restart: on-failure
    networks:
      - trailmix-network
    # This service extracts the URL when cloudflared starts

  frontend:
    build:
      context: ./trailmix
      dockerfile: Dockerfile
    container_name: trailmix-frontend
    ports:
      - "8081:8081"
    volumes:
      - ./trailmix:/app
      - /app/node_modules
      - ./secrets:/app/secrets:ro
      # Mount Docker socket so frontend can read cloudflared logs
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      # This will be overridden by the entrypoint script with the cloudflared tunnel URL
      - EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
      - EXPO_USE_TUNNEL=true
      # Force unbuffered output for better QR code visibility
      - PYTHONUNBUFFERED=1
      - NODE_ENV=development
      # Force color output and ensure proper terminal handling
      - FORCE_COLOR=1
      - TERM=xterm-256color
      - CI=false
    depends_on:
      backend:
        condition: service_healthy
      cloudflared:
        condition: service_started
      tunnel-url-extractor:
        condition: service_completed_successfully
    restart: unless-stopped
    stdin_open: true
    tty: true
    # Ensure logs are shown in real-time
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - trailmix-network

volumes:
  postgres_data:
  redis_data:

networks:
  trailmix-network:
    driver: bridge

